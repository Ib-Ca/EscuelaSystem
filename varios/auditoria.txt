                        const tabla = "Alumnos";
                        const cambio = "Creación";
                        const datos_anteriores = "";
                        Auditoria(idUsuario, tabla, cambio, datos_anteriores); JSON.stringify(datosAnteriores));

idUsuario: User.user.idusuario
const idUsuario = req.body.idUsuario;

const idUsuario = req.params.idUsuario;





db.query(
    "SELECT * FROM alumnos WHERE idAlumnos = ?",
    [idAlumno],
    function (err, resultAnterior) {
      if (err) {
        console.log(err);
        return res.status(500).send("Error al obtener datos anteriores del alumno");
      } else {
        // Guarda los datos anteriores en un objeto
        const datosAnteriores = resultAnterior[0];
    }
          }
        );